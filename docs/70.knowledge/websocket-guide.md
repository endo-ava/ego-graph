# WebSocket & Terminal Gateway ガイド

## Status

このガイドは、EgoGraph が terminal runtime を内包していた時期の知識メモです。
現在の terminal/runtime 実装は **Plexus** 側へ移管済みであり、この repo の運用ガイドとしては使用しません。

## 現在の扱い

- WebSocket 自体の基礎知識として読むのはよい
- `gateway.main`、`ws/terminal`、tmux 接続手順などの具体的な運用手順は **EgoGraph では無効**
- 現行の terminal/runtime 実装修正は Plexus 側で行う
