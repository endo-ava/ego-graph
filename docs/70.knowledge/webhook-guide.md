# Webhook & Push Notification ガイド

## Status

このドキュメントは、EgoGraph 内に Gateway runtime が存在した時期の運用メモです。
push / webhook runtime は 2026-03 時点で **Plexus** 側へ移管済みであり、この repo の実行手順としては利用しません。

## 現在の扱い

- Webhook の概念説明としては参照可能
- `POST /v1/push/webhook` や `gateway` への curl 例は EgoGraph では現行手順ではない
- 運用・設定・認証方式の最新情報は Plexus 側で管理する
